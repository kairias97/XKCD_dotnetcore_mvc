@model DisplayedComicVM

<div class="row">
    <div class="col-12">

        <partial name="_ComicNavigationPartial" model="@Model?.Navigation" />
        <div class="text-center">
            <h3>@Model?.Comic?.Title</h3>
            <img class="img-fluid" alt="@Model?.Comic?.Alt" src="@Model?.Comic?.Img" /><br /><br />
            <p>@Model?.Comic?.Alt</p>
        </div>
        <partial name="_ComicNavigationPartial" model="@Model?.Navigation" />
    </div>
</div>
